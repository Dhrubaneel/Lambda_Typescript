let STUB = 1;
/**
 * Type definition for geometric figure
 * @typedef geometricFigure
 * @type {Object}
 * @property {number} width - width of the figure
 * @property {number} height - height of the figure
 */
export type geometricFigure = {
    width: number;
    height: number;
}
STUB = 1;

/**
 * Type definition for dimension result
 * @typedef {Object} dimensionResult
 * @property {number} area - area of the figure
 * @property {number} perimeter - perimeter of the figure
 */
export type dimensionResult = {
    area: number;
    perimeter: number;
}
